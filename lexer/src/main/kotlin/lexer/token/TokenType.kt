package lexer.token

enum class TokenType {

    //ASSIGNATION
    ASSIGNATION,

    IDENTIFIER,

    //KEYWORDS
    LET,
    PRINTLN,

    //TYPES
    STRINGTYPE,
    NUMBERTYPE,

    //OPERATIONS
    PLUS,
    MINUS,
    TIMES,
    DIVIDEDBY,
    OPENPAREN,
    CLOSEPAREN,

    //DECLARATIONS
    NUMERIC_LITERAL,
    STRING_LITERAL,
    REGULAR_EXPRESSION_LITERAL,

    //EVALUATORS
    EQUALS,
    GREATER_THAN,
    LESSER_THAN,
    DIFFERENT,
}